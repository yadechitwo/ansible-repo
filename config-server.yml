---
- name: Create dest folder
  hosts: all
  become: yes
  become_user: admin
  vars:
    dest: "/var/www/site/"
  tasks:

    - name: create directory for website
      file:
        path: /var/www/site/
        state: directory
        mode: 0755